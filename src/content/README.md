# Site content

All words on the site live here, separate from the layout code. Edit these
files; the pages read from them.

- `copy/home.md` — homepage: opening line, intro, section headings
- `copy/about.md` — About page: bio, facts, background, speaker kit
- `copy/cta.md` — the closing call to action on every page
- `data/research.yaml` — papers
- `data/projects.yaml` — tools
- `data/talks.yaml` — upcoming and past talks
- `data/writing.yaml` — articles (own site and outside bylines)
- `data/recognition.yaml` — independent citations and coverage
- `data/profiles.yaml` — the /links page

## Draft conventions

- `[TBC: ...]` marks something only you can confirm or decide. Nothing on
  the site goes live while a `[TBC` remains.
- `# SOURCE:` comments record where a fact was checked, so it can be
  re-checked later.
- Quotes from other people are copied verbatim from the live page on the
  date noted. Don't paraphrase them.
